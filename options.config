[DEFAULT]

# 模式 1=浏览器半模拟 2=接口直发
mode = 2

# sdut统一认证
sdut_account =
sdut_pwd =

# 账号
username =
# 密码
password =
# 接口直发模式下必填 密码被加密，从/api/sys/user/bind/wechat?sf_request_type=ajax的Payload的name字段获取
password_encoded =

# 预约策略 仅室内=1 仅室外=2 室内室外都预约=3
policy =